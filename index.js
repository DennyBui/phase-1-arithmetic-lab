let num1 = 31;
let num2 = 2;
let random = Math.ceil(Math.random() * 10 + 1); //generates a random number between 1 and 10
let num3 = 20;
let num4 = 8;
let set = [5, 10, 15, 20]
let max = Math.max(5, 10, 15, 20)
const multiply = num1 * num2
const mod = num3 % num4
multiply(num1, num2) 
function random() {
    return Math.floor(Math.random() * 10) + 1;
};
mod(num3, num4) 
function max(set) {
    return Math.max(5, 10, 15, 20)
};